{
  "name": "azure-copilot-finops-starter",
  "version": "0.1.0",
  "private": false,
  "description": "Human-governed FinOps starter contracts and workflow core",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "demo": "npm run build && node dist/demo.js",
    "clean": "powershell -NoProfile -Command \"if (Test-Path dist) { Remove-Item -Recurse -Force dist }\""
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.6.3"
  }
}
